# mycourses — agent guide

Configuration for the Arc engineering skills. Details live in `docs/agents/`.

## Agent skills

### Issue tracker

GitHub Issues, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The six canonical triage roles, using their default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Ship style

Direct push to `main` — no pull requests. See `docs/agents/ship-style.md`.
